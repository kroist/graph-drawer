# Graph drawer
example usage:

    build/./drawer --in_file examples/0.json --in_json --transform full --out_type normal,tikz

to compile project, do

    cmake -B build/ -S .

    cmake --build build

to run the tests, do

    build/./tests

